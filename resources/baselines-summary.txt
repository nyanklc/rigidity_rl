=================================================================================================================
BASELINE COMPARISON
=================================================================================================================
  environment : env_actionAddRemoveEdgeDiscreteNoSelfLoops_rewardWeightedNormalized_termMaxSteps_rigGFE_lean_n8_R3
  network     : 8 agents in R^3, action space AddRemoveEdgeDiscreteNoSelfLoops
  objective   : WeightedNormalized state score
  instances   : 20 networks from benchmark bench_n8_R3 (a678a0266a20)
  policy      : generaldqngine (DQN, --policy-mode sample, 50-step budget)

  method          edges         score   rigid  minimal           rigidity    rigidity(geo)       work       best@
                (fewer)      (higher)       %        %           mean+-sd      gmean x/gsd      edits        step
  ---------------------------------------------------------------------------------------------------------------
  initial   11.20+-5.33  48.50+-16.96      35        0   2.5e-02+-5.1e-02   4.5e-02 x/2.9*          -           -
  random    13.85+-2.03   63.38+-4.56      65        0   3.7e-02+-5.7e-02   2.9e-02 x/3.6*  23.2+-2.7    9.7+-9.9
  greedy    10.50+-0.50   73.75+-1.25     100       50   8.5e-03+-1.3e-02    2.8e-03 x/5.3   6.2+-2.5    6.2+-2.5
  learned   10.05+-0.22   74.88+-0.54     100       95   5.2e-03+-1.0e-02    1.2e-03 x/7.8  50.0+-0.0    8.0+-3.4

-----------------------------------------------------------------------------------------------------------------
WHAT THE METHODS ARE
  initial   the random graph each method starts from
  random    uniform random actions - the floor any method should beat
  greedy    repeatedly applies the single best edge change until none helps
  learned   the trained policy

WHAT THE COLUMNS MEAN
  edges     how many directed bearing measurements the final network needs.
            Each edge is a sensor/communication link, so fewer is better.
  score     the objective every method is scored with (phi). Higher is better;
            it rewards rigidity and penalises each extra edge.
  rigid     % of networks whose shape is fully determined by its bearing
            measurements. This is the property being solved for.
  minimal   % that are rigid AND use the fewest possible edges.
            (heuristic on mixed-domain networks - may under-report)
  rigidity  how much margin the network has before it stops being rigid.
            Larger is more robust. Its absolute size depends on how far
            apart the agents are, so compare rows rather than the number.
  rigidity(geo)
            the same margin as a geometric mean and spread, because it
            ranges over orders of magnitude: 'a x/b' means the typical
            network sits between a/b and a*b. A '*' marks rows where
            non-rigid networks (margin exactly 0) had to be left out --
            a zero cannot enter a geometric mean, so those rows describe
            only the networks that came out rigid.
  work      how many changes to the network the method actually made.
  best@     the step at which its best network was found. Lower means it
            converged faster; the rest of the budget added nothing.

HOW TO READ IT
  Every value is a mean over the networks; '+-' is the standard deviation
  across them, i.e. how much the method varies from one network to the next.
  The percentage columns carry no '+-': they already are means of a yes/no
  outcome, whose spread is fixed by the percentage itself.
  'initial' and 'optimal' are reference points, not competing methods:
  every method starts from 'initial', and 'optimal' is the best achievable.
  A method is doing well when it approaches 'optimal' with low 'work'.
  All methods are run on the same networks, so rows compare directly.
=================================================================================================================
